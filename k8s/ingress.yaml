apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: promotion-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: promotion
                port:
                  number: 8000
